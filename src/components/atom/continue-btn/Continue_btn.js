import React from 'react'

const continue_btn = (props) => {
    return (
        <div className="continue-btn-1">
            <button>{props.cont_btn}</button>
        </div>
    )
}

export default continue_btn
